[build-system]
requires = ["setuptools>=77.0"]
build-backend = "setuptools.build_meta"

[project]
name = "dotvector"
description = "Python native ctypes connector for DotVector embedded vector database"
version = "0.2.0"
readme = "README.md"
license = {text = "MIT"}
requires-python = ">=3.10"
dependencies = []
