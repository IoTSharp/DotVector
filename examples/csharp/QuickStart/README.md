# QuickStart

Run this sample with:

```bash
dotnet run --project examples/csharp/QuickStart/QuickStart.csproj
```

It creates an in-memory collection, inserts a few vectors, and prints the top matches for a sample query.
